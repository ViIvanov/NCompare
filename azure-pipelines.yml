trigger:
- main
- feature/*

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildConfiguration: 'Release'

steps:

- task: UseDotNet@2
  displayName: 'Install .NET'
  inputs:
    version: '7.x'
    performMultiLevelLookup: true

- task: DotNetCoreCLI@2
  displayName: 'Build solution'
  inputs:
    command: 'build'
    projects: '$(solution)'
    arguments: '--configuration "$(buildConfiguration)" --version-suffix "ci" --output "$(Build.BinariesDirectory)"'

- task: DotNetCoreCLI@2
  displayName: 'Run tests'
  inputs:
    command: 'test'
    projects: '$(Build.BinariesDirectory)'
    arguments: '--no-build --configuration "$(buildConfiguration)" --collect "Code coverage"'

- task: CopyFiles@2
  displayName: 'Copy articacts'
  inputs:
    SourceFolder: '$(Build.BinariesDirectory)'
    Contents: |
      NCompare.*.nupkg
      NCompare.*.snupkg
    TargetFolder: '$(Build.StagingDirectory)'

- task: PublishPipelineArtifact@1
  displayName: 'Publish artifacts'
  inputs:
    targetPath: '$(Build.StagingDirectory)'
    artifact: 'Packages'
